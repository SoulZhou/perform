==============================================ToolUtil=================================================
2017年12月7日 16:52:06
	1.发现想封装上网权限的名单设置，但是编译的时候不能编译到apk里面，导致运行的时候直接找不到DroidWallAPI
	所以放弃封装了 貌似jar包里面在导入jar包的方式不行
	
2016-9-12
	1、新增一个用于xml解析的模版工具类 利用sax
	2、目前里面只有一些简单的数据解析，比如整形或者字符串，但是我们后面可以自己添加一些数据类型
	比如自定义的开头啊，结尾啊 或者其它的数据类型
	
2016-9-8
	1、在print里面现在把是否打印的判断去掉了，然后屏幕打印也新增上去，然后做了一个开关用
	/mnt/sdcar/logdebug.txt文件
	
2016-1-20
	1、以后的打印就全部调用这个类里面的，不做其它的了，切记，切记